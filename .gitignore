# Python
__pycache__/
*.py[cod]
*.egg-info/
.venv/
venv/
env/
.ipynb_checkpoints/

# Dữ liệu sinh ra / mô hình đã train (không commit, tái tạo được từ notebook/script)
models/*.joblib
models/*.pkl
reports/**/*.png
reports/**/*.csv
reports/**/*.json
!reports/**/.gitkeep
!models/.gitkeep
# giữ lại các báo cáo .md chốt trong reports/ (BAO_CAO_*.md)

# Dataset lớn tải thêm (giữ dataset_phishing.csv gốc)
Dataset/reference/
Dataset/external/
Dataset/simulated/

# Script trích đặc trưng gốc tải từ Mendeley (bản quyền tác giả — không redistribute)
src/features/hannousse_yahiouche/

# Công cụ cộng tác markdown (chứa token phiên cục bộ — không commit)
.markdown-collab/

# IDE / OS
.idea/
.vscode/
.DS_Store
Thumbs.db

# Bí mật
.env
*.key
